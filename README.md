# Packet-Capturing-and-Session-Hijacking
During a security audit for a local business, I discovered that internal web applications were running over HTTP instead of HTTPS, exposing the network to potential attacks. I demonstrated how an attacker with access to the corporate network could capture packets, perform active sniffing, and carry out man-in-the-middle attacks.  
